# This file is automatically generated from cmake.toml - DO NOT EDIT
# See https://github.com/build-cpp/cmkr for more information

cmake_minimum_required(VERSION 3.30)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_BINARY_DIR)
	message(FATAL_ERROR "In-tree builds are not supported. Run CMake from a separate directory: cmake -B build")
endif()

# Enable support for MSVC_RUNTIME_LIBRARY
cmake_policy(SET CMP0091 NEW)

set(CMKR_ROOT_PROJECT OFF)
if(CMAKE_CURRENT_SOURCE_DIR STREQUAL CMAKE_SOURCE_DIR)
	set(CMKR_ROOT_PROJECT ON)

	# Bootstrap cmkr and automatically regenerate CMakeLists.txt
	include("cmake/cmkr.cmake" OPTIONAL RESULT_VARIABLE CMKR_INCLUDE_RESULT)
	if(CMKR_INCLUDE_RESULT)
		cmkr()
	endif()

	# Enable folder support
	set_property(GLOBAL PROPERTY USE_FOLDERS ON)

	# Create a configure-time dependency on cmake.toml to improve IDE support
	set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS cmake.toml)
	if(NOT DEFINED CMAKE_MSVC_RUNTIME_LIBRARY)
		set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
	endif()

endif()

# vcpkg settings
set(VCPKG_OVERLAY_PORTS
	"${CMAKE_CURRENT_SOURCE_DIR}/vcpkg-overlay"
)
set(VCPKG_OVERLAY_TRIPLETS
	"${CMAKE_CURRENT_SOURCE_DIR}/vcpkg-overlay"
)
project(MegaVoltsPP
	LANGUAGES
		C
		CXX
)

if(CMKR_ROOT_PROJECT AND NOT CMKR_DISABLE_VCPKG)
	include(FetchContent)
	# Fix warnings about DOWNLOAD_EXTRACT_TIMESTAMP
	if(POLICY CMP0135)
		cmake_policy(SET CMP0135 NEW)
	endif()
	message(STATUS "Fetching vcpkg (2025.06.13)...")
	FetchContent_Declare(vcpkg
		URL
			"https://github.com/microsoft/vcpkg/archive/refs/tags/2025.06.13.tar.gz"
		SUBBUILD_DIR
			"CMakeFiles/vcpkg-subbuild"
		SOURCE_DIR
			vcpkg
		BINARY_DIR
			"CMakeFiles/vcpkg-build"
	)
	FetchContent_MakeAvailable(vcpkg)

	if(CMAKE_HOST_SYSTEM_NAME STREQUAL Darwin AND CMAKE_OSX_ARCHITECTURES STREQUAL "")
		set(CMAKE_OSX_ARCHITECTURES ${CMAKE_HOST_SYSTEM_PROCESSOR} CACHE STRING "" FORCE)
	endif()
	include("${vcpkg_SOURCE_DIR}/scripts/buildsystems/vcpkg.cmake")
endif()

# Packages
find_package(fmt REQUIRED)

find_package(crashpad REQUIRED)

find_package(unofficial-mariadb-connector-cpp REQUIRED)

# Subdirectory: deps
set(CMKR_CMAKE_FOLDER ${CMAKE_FOLDER})
if(CMAKE_FOLDER)
	set(CMAKE_FOLDER "${CMAKE_FOLDER}/deps")
else()
	set(CMAKE_FOLDER deps)
endif()
add_subdirectory(deps)
set(CMAKE_FOLDER ${CMKR_CMAKE_FOLDER})

# Subdirectory: common
set(CMKR_CMAKE_FOLDER ${CMAKE_FOLDER})
if(CMAKE_FOLDER)
	set(CMAKE_FOLDER "${CMAKE_FOLDER}/common")
else()
	set(CMAKE_FOLDER common)
endif()
add_subdirectory(common)
set(CMAKE_FOLDER ${CMKR_CMAKE_FOLDER})

# Target: front
set(front_SOURCES
	cmake.toml
	"src/Front/CFrontServer.cpp"
	"src/Front/CFrontServer.h"
	"src/Front/handlers/ChannelsInfo.h"
	"src/Front/handlers/LoginAuth.h"
	"src/Front/handlers/LoginReconnect.h"
	"src/Front/handlers/ServerConnect.h"
	"src/Front/handlers/ServerDisconnect.h"
	"src/Front/handlers/ServerIpcMessage.h"
	"src/Front/main.cpp"
)

add_executable(front)

target_sources(front PRIVATE ${front_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${front_SOURCES})

target_compile_definitions(front PRIVATE
	RELEASE_1_0_3
	_CRT_SECURE_NO_WARNINGS
	_WIN32_WINNT=0x0A00
	MARIADB_STATIC_LINK
)

target_compile_features(front PRIVATE
	cxx_std_23
)

if(MSVC) # msvc
	target_compile_options(front PRIVATE
		"/bigobj"
		"/MP"
		"/wd4661"
		"/utf-8"
	)
endif()

if((CMAKE_CXX_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_CXX_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$") OR (CMAKE_C_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_C_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$")) # clang
	target_compile_options(front PRIVATE
		-finput-charset=utf-8
		-fexec-charset=utf-8
		-Wa,-mbig-obj
		-Wno-some-warning
		-stdlib=libc++
	)
endif()

target_link_libraries(front PRIVATE
	secur32
	crypt32
	crypt32
	asio
	boost_unordered
	thread-pool
	rapidjson
	monocypher
	fmt::fmt
	crashpad::crashpad
	unofficial::mariadb-connector-cpp::mariadbcpp
	common
)

if((CMAKE_CXX_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_CXX_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$") OR (CMAKE_C_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_C_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$")) # clang
	target_link_options(front PRIVATE
		-fuse-ld=lld
		"-Wl,-L/usr/local/lib/"
	)
endif()

target_include_directories(front PRIVATE
	.
)

get_directory_property(CMKR_VS_STARTUP_PROJECT DIRECTORY ${PROJECT_SOURCE_DIR} DEFINITION VS_STARTUP_PROJECT)
if(NOT CMKR_VS_STARTUP_PROJECT)
	set_property(DIRECTORY ${PROJECT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT front)
endif()

set(CMKR_TARGET front)
install(TARGETS front RUNTIME DESTINATION bin)

# Target: main
set(main_SOURCES
	cmake.toml
	"src/Main/CMainServer.cpp"
	"src/Main/CMainServer.h"
	"src/Main/handlers/BossBattleRespawn.h"
	"src/Main/handlers/BuyItem.h"
	"src/Main/handlers/ChangeTeam.h"
	"src/Main/handlers/ChannelsInfo.h"
	"src/Main/handlers/CreateParty.h"
	"src/Main/handlers/CreateRoom.h"
	"src/Main/handlers/DeleteItem.h"
	"src/Main/handlers/EndMatch.h"
	"src/Main/handlers/EquipItem.h"
	"src/Main/handlers/GachaponSpin.h"
	"src/Main/handlers/GameEventMessage.h"
	"src/Main/handlers/JoinPlaza.h"
	"src/Main/handlers/JoinRoom.h"
	"src/Main/handlers/LeaveMatch.h"
	"src/Main/handlers/LeaveParty.h"
	"src/Main/handlers/LeavePlaza.h"
	"src/Main/handlers/LeaveRoom.h"
	"src/Main/handlers/LobbyUserDetails.h"
	"src/Main/handlers/LobbyUserList.h"
	"src/Main/handlers/NextRoundUpdateMatch.h"
	"src/Main/handlers/PackageOpen.h"
	"src/Main/handlers/PlayerAuthorize.h"
	"src/Main/handlers/PlayerChangeCharacter.h"
	"src/Main/handlers/PlayerChat.h"
	"src/Main/handlers/PlayerEnergy.h"
	"src/Main/handlers/PlayerMailbox.h"
	"src/Main/handlers/PlayerMissions.h"
	"src/Main/handlers/PlayerNameChange.h"
	"src/Main/handlers/PlayerPickupDrop.h"
	"src/Main/handlers/PlayerPing.h"
	"src/Main/handlers/PlayerSocials.h"
	"src/Main/handlers/RepairItem.h"
	"src/Main/handlers/SellItem.h"
	"src/Main/handlers/ServerConnect.h"
	"src/Main/handlers/ServerDisconnect.h"
	"src/Main/handlers/ServerIpcMessage.h"
	"src/Main/handlers/StartMatch.h"
	"src/Main/handlers/UpdateRoomList.h"
	"src/Main/handlers/UpdateRoomSettings.h"
	"src/Main/handlers/UpgradeItem.h"
	"src/Main/main.cpp"
)

add_executable(main)

target_sources(main PRIVATE ${main_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${main_SOURCES})

target_compile_definitions(main PRIVATE
	RELEASE_1_0_3
	_CRT_SECURE_NO_WARNINGS
	_WIN32_WINNT=0x0A00
	MARIADB_STATIC_LINK
)

target_compile_features(main PRIVATE
	cxx_std_23
)

if(MSVC) # msvc
	target_compile_options(main PRIVATE
		"/bigobj"
		"/MP"
		"/wd4661"
		"/utf-8"
	)
endif()

if((CMAKE_CXX_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_CXX_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$") OR (CMAKE_C_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_C_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$")) # clang
	target_compile_options(main PRIVATE
		-finput-charset=utf-8
		-fexec-charset=utf-8
		-Wa,-mbig-obj
		-Wno-some-warning
		-stdlib=libc++
	)
endif()

target_link_libraries(main PRIVATE
	secur32
	crypt32
	crypt32
	asio
	boost_unordered
	thread-pool
	rapidjson
	monocypher
	fmt::fmt
	crashpad::crashpad
	unofficial::mariadb-connector-cpp::mariadbcpp
	common
)

if((CMAKE_CXX_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_CXX_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$") OR (CMAKE_C_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_C_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$")) # clang
	target_link_options(main PRIVATE
		-fuse-ld=lld
		"-Wl,-L/usr/local/lib/"
	)
endif()

target_include_directories(main PRIVATE
	.
)

get_directory_property(CMKR_VS_STARTUP_PROJECT DIRECTORY ${PROJECT_SOURCE_DIR} DEFINITION VS_STARTUP_PROJECT)
if(NOT CMKR_VS_STARTUP_PROJECT)
	set_property(DIRECTORY ${PROJECT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT main)
endif()

set(CMKR_TARGET main)
install(TARGETS main RUNTIME DESTINATION bin)

# Target: cast
set(cast_SOURCES
	cmake.toml
	"src/Cast/CCastServer.cpp"
	"src/Cast/CCastServer.h"
	"src/Cast/handlers/ConnectionAuth.h"
	"src/Cast/handlers/CreateRoom.h"
	"src/Cast/handlers/HostAbility.h"
	"src/Cast/handlers/HostExplosives.h"
	"src/Cast/handlers/HostExplosivesDamage.h"
	"src/Cast/handlers/HostPickupItem.h"
	"src/Cast/handlers/HostRespawn.h"
	"src/Cast/handlers/HostSpawnAmmo.h"
	"src/Cast/handlers/HostSyncTick.h"
	"src/Cast/handlers/HostTurnZombie.h"
	"src/Cast/handlers/HostUpdateTick.h"
	"src/Cast/handlers/HostWeaponAttack.h"
	"src/Cast/handlers/LeaveRoom.h"
	"src/Cast/handlers/NpcsAttack.h"
	"src/Cast/handlers/NpcsMovement.h"
	"src/Cast/handlers/NpcsProjectiles.h"
	"src/Cast/handlers/NpcsSpawn.h"
	"src/Cast/handlers/PingPong.h"
	"src/Cast/handlers/PlayerAbility.h"
	"src/Cast/handlers/PlayerCrash.h"
	"src/Cast/handlers/PlayerEndMatch.h"
	"src/Cast/handlers/PlayerJoinPlaza.h"
	"src/Cast/handlers/PlayerJoinRoom.h"
	"src/Cast/handlers/PlayerLeaveMatch.h"
	"src/Cast/handlers/PlayerLeavePlaza.h"
	"src/Cast/handlers/PlayerLoadMatch.h"
	"src/Cast/handlers/PlayerMovement.h"
	"src/Cast/handlers/PlayerNicknames.h"
	"src/Cast/handlers/PlayerPickupItem.h"
	"src/Cast/handlers/PlayerReload.h"
	"src/Cast/handlers/PlayerRespawn.h"
	"src/Cast/handlers/PlayerScoreMatch.h"
	"src/Cast/handlers/PlayerSpawnBomb.h"
	"src/Cast/handlers/PlayerStartMatch.h"
	"src/Cast/handlers/PlayerSyncTick.h"
	"src/Cast/handlers/PlayerUnknown1.h"
	"src/Cast/handlers/PlayerUnknown2.h"
	"src/Cast/handlers/PlayerUnknown3.h"
	"src/Cast/handlers/PlayerUnknown4.h"
	"src/Cast/handlers/PlayerUnknown5.h"
	"src/Cast/handlers/PlayerUnknown6.h"
	"src/Cast/handlers/PlayerUnknown7.h"
	"src/Cast/handlers/PlayerUnknown8.h"
	"src/Cast/handlers/PlayerWeaponAttack.h"
	"src/Cast/handlers/ServerConnect.h"
	"src/Cast/handlers/ServerDisconnect.h"
	"src/Cast/handlers/ServerIpcMessage.h"
	"src/Cast/main.cpp"
)

add_executable(cast)

target_sources(cast PRIVATE ${cast_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${cast_SOURCES})

target_compile_definitions(cast PRIVATE
	RELEASE_1_0_3
	_CRT_SECURE_NO_WARNINGS
	_WIN32_WINNT=0x0A00
	MARIADB_STATIC_LINK
)

target_compile_features(cast PRIVATE
	cxx_std_23
)

if(MSVC) # msvc
	target_compile_options(cast PRIVATE
		"/bigobj"
		"/MP"
		"/wd4661"
		"/utf-8"
	)
endif()

if((CMAKE_CXX_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_CXX_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$") OR (CMAKE_C_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_C_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$")) # clang
	target_compile_options(cast PRIVATE
		-finput-charset=utf-8
		-fexec-charset=utf-8
		-Wa,-mbig-obj
		-Wno-some-warning
		-stdlib=libc++
	)
endif()

target_link_libraries(cast PRIVATE
	secur32
	crypt32
	crypt32
	asio
	boost_unordered
	thread-pool
	rapidjson
	monocypher
	fmt::fmt
	crashpad::crashpad
	unofficial::mariadb-connector-cpp::mariadbcpp
	common
)

if((CMAKE_CXX_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_CXX_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$") OR (CMAKE_C_COMPILER_ID MATCHES "Clang" AND NOT CMAKE_C_COMPILER_FRONTEND_VARIANT MATCHES "^MSVC$")) # clang
	target_link_options(cast PRIVATE
		-fuse-ld=lld
		"-Wl,-L/usr/local/lib/"
	)
endif()

target_include_directories(cast PRIVATE
	.
)

get_directory_property(CMKR_VS_STARTUP_PROJECT DIRECTORY ${PROJECT_SOURCE_DIR} DEFINITION VS_STARTUP_PROJECT)
if(NOT CMKR_VS_STARTUP_PROJECT)
	set_property(DIRECTORY ${PROJECT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT cast)
endif()

set(CMKR_TARGET cast)
install(TARGETS cast RUNTIME DESTINATION bin)
