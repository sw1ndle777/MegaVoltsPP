# This file is automatically generated from cmake.toml - DO NOT EDIT
# See https://github.com/build-cpp/cmkr for more information

# Create a configure-time dependency on cmake.toml to improve IDE support
if(CMKR_ROOT_PROJECT)
	set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS cmake.toml)
endif()

# Target: asio
add_library(asio INTERFACE)

target_include_directories(asio INTERFACE
	"asio/asio/include"
)

# Target: boost_unordered
add_library(boost_unordered INTERFACE)

target_include_directories(boost_unordered INTERFACE
	boost_unordered
)

# Target: thread-pool
add_library(thread-pool INTERFACE)

target_include_directories(thread-pool INTERFACE
	"thread-pool/include"
)

# Target: magic_enum
add_library(magic_enum INTERFACE)

target_include_directories(magic_enum INTERFACE
	magic_enum
)

# Target: rapidjson
add_library(rapidjson INTERFACE)

target_include_directories(rapidjson INTERFACE
	"rapidjson/include"
)

# Target: monocypher
set(monocypher_SOURCES
	"monocypher/src/monocypher.c"
	"monocypher/src/monocypher.h"
	"monocypher/src/optional/monocypher-ed25519.c"
	"monocypher/src/optional/monocypher-ed25519.h"
)

add_library(monocypher INTERFACE)

target_sources(monocypher INTERFACE ${monocypher_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${monocypher_SOURCES})

target_include_directories(monocypher INTERFACE
	"monocypher/src"
)
